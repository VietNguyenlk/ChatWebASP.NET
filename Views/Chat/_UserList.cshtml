@model List<ChatWeb.Models.ApplicationUser>

<h5 class="mt-3">Danh sách người dùng</h5>
<ul class="list-group">
    @foreach (var user in Model)
    {
        <li class="list-group-item">
            <a asp-action="Home" asp-route-userId="@user.Id">@user.UserName</a>
        </li>
    }
</ul>
